This folder contains the sample database used by the hybrid agent.

To create the sample database locally run:

```powershell
python data/create_sample_db.py
```

This will create `northwind.sqlite` with a few small tables useful for quick testing.
