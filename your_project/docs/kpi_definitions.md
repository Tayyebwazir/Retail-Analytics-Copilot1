# KPI Definitions

Use this doc to define business KPIs and formulas the agent may reference.

- AOV (Average Order Value): sum(order_total) / count(orders)
- Units per Transaction: total_units / count(orders)
- Conversion Rate: purchases / visitors
